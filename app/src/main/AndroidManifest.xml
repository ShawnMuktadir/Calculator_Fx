<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.mdmuktadir.calculator_fx">

    <application
        android:allowBackup="true"
        android:icon="@drawable/cl"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Base.V23.Theme.AppCompat.Light"
        android:largeHeap="true">

        <activity android:name=".LauncherActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity" />
        <activity android:name=".UnitArea" />
        <activity android:name=".UnitConverter" />
        <activity android:name=".UnitLength" />
        <activity android:name=".UnitTemperature" />
        <activity android:name=".UnitWeight" />
        <activity android:name=".SciCalcActivity"></activity>
    </application>

</manifest>